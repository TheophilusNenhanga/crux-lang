let list1 = [0, 1];let last_index = 1;let time_start = time_s();let duration = 10;let computations = 0;while time_s() - time_start < duration {array_add(list1, list1[last_index-1] + list1[last_index]);last_index = last_index + 1;computations = computations + 1;}print(a);print("Completed Computations: ");print(computations);
